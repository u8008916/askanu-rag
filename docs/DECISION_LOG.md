# DECISION_LOG.md

Record only decisions that change V3 contracts, architecture, source policy, scope, security/privacy rules, or schedule.

| Date | Decision | Why | Affected repos/docs | Owner | Approved by |
|---|---|---|---|---|---|
| 2026-09-05 | Freeze HTTP 400 for malformed JSON/validation, 413 for oversized input, 429 for rate limiting, and controlled 5xx JSON for DB/model/internal dependency failures; retain `error` status and safe response envelope. | Unblock Day 1 contract validation without changing V3 statuses. | RAG `docs/API_CONTRACT.md`; App contract copy requires synchronisation. | Qasim (contracts) | User instruction in pre-bootstrap cleanup request |
| 2026-09-05 | Define non-null pending clarification using the existing clarification object; distinguish evidence `record_id` from registry `source_id`; define minimal Events/Jobs list responses with V3 ordering and stored-record URLs. | Remove integration ambiguity while preserving V3 decisions; defer domain field types/nullability to Day 2. | RAG `docs/API_CONTRACT.md`; App/Scraper affected contract copies require synchronisation. | Qasim (contracts) | User instruction in pre-bootstrap cleanup request |
| 2026-09-06 | Freeze Courses/Programs schema v1: full 16-field normalized Scraper -> DB -> RAG record; year-scoped `entity_id`; namespaced stable `record_id`; explicit four-digit academic year; SHA-256 `content_hash` of canonical `content`; stored canonical URLs; strict no-invented-value/null policy; multi-year-safe RAG lookup; and first DB/migration ownership. | Unblock Will -> Carmen integration using one implementation-ready shared boundary based on the verified scraper output and Carmen's Day 2 exact-retrieval requirements. | RAG `docs/DATA_SCHEMA.md` and `docs/DECISION_LOG.md`; Scraper `docs/DATA_SCHEMA.md` and implementation require synchronisation before PR #5 merge. | Qasim (contracts/integration) | Qasim after Will/Carmen implementation review |
| 2026-09-11 | Keep shared DB migrations in the RAG repo; persist the frozen `ingestion_runs` contract; assign scraper upsert/change detection/run writes to Will; use `PENDING` as the indexing handoff while preserving index state/version for `UNCHANGED` records. | Provide durable run-count audit and one cross-repo indexing signal without creating a second migration owner or duplicating scraper logic in RAG. | RAG migration, `docs/DATA_SCHEMA.md`, `docs/DEPLOYMENT.md`, README; Scraper implementation/contract copy requires synchronisation. | Qasim (contracts/integration) | Qasim final Day 7 ownership decision |
| 2026-09-11 | Log a validated App `X-Request-Id` separately from RAG's API `request_id`, and require the Day 7 Cloud Run revision to attach the confirmed Cloud SQL instance explicitly. | Preserve end-to-end correlation without weakening the frozen response identity, and ensure the configured `/cloudsql/...` Unix socket is actually mounted rather than relying on IAM alone. | RAG request logging/tests, `docs/DEPLOYMENT.md`, README; App request-header integration and Qasim deployment workflow. | Qasim (contracts/integration) | Qasim final PR #16 integration fixes |
| 2026-09-12 | Clarify the existing current-session conversation contract for the four merged App guided-card prompts: an empty session missing a course or program/degree returns `needs_clarification` with the minimum missing detail; the honours prompt explicitly does not offer eligibility checking. | Align the frozen envelope and session-only safety model with the merged App entry points without adding public intent/session fields, persistent storage, a degree audit, or an honours eligibility engine. | RAG `src/askanu_rag/conversation.py`, conversation tests, `docs/CONVERSATION_CONTRACT.md`, V5 handoff; App guided-card/copy integration. | Carmen (RAG/backend) | Qasim, PR #18 pre-merge review (approval relayed in user instruction) |
| 2026-09-13 | Freeze the Day 9 shared-persistence generalisation: rename the deployed physical table in place to writable `source_records`, expose `course_program_records` as a Courses/Programs compatibility read view, enforce Scholarship URL-slug identity with literal final-segment equality, and add only the approved Scholarships source/domain and metadata contract in new revision `20260913_0002`. | Preserve all existing rows and the 16-field boundary while avoiding a Scholarship-only table, duplicated authority, or record-controlled regex identity checks. | RAG migration/models/repositories and shared docs; Will's scraper PostgreSQL target and shared contract copy require synchronisation. | Carmen (RAG/backend) | Qasim table/view naming, ownership, live permissions and migration execution approval |
| 2026-09-13 | Freeze Scholarship canonical identity as `https://study.anu.edu.au/scholarships/find-scholarship/<slug>` with slug grammar `^[a-z0-9]+(?:-[a-z0-9]+)*$` and literal final-slug/`entity_id` equality; keep opening/closing dates in metadata while freezing top-level effective dates as null; freeze first-collection/freshness timestamps, bounded-run atomicity and unknown-commit reconciliation; and make the scraper authoritative for canonical content/hash while RAG validates format and trusts the persisted invariant. | Align RAG and scraper at one testable persistence boundary, prevent alternate URL identities and partial-success runs, and avoid two independent hash normalizers. | RAG migration/model/tests and `DATA_SCHEMA`/deployment/handoff docs; Will's scraper writer and shared contract copy. | Qasim (contracts/integration) | Qasim after Will cross-repository review (approval relayed in PR #19 contract-alignment follow-up) |
| 2026-09-14 | Add follow-up revision `20260914_0003` to recreate only `course_program_records` with a semantics-neutral top-level `OFFSET 0`, making the compatibility view structurally non-updatable while preserving its exact 16-column Courses-only reads and retaining live permission tests as a release gate. | Qasim's PostgreSQL 18 integration check showed the `0002` simple view was automatically updatable and insertable; the compatibility boundary must reject writes independently of application intent without changing stored data, projected columns, or the Scholarship/API contracts. | RAG migration tests, `docs/DEPLOYMENT.md`, and Day 9 handoff. | Carmen (RAG/backend) | Qasim live PostgreSQL 18 integration follow-up (relayed in user instruction) |
| 2026-09-14 | Freeze Jobs normalized contract v1 on shared `source_records`: `jobs_anu_search`/`jobs`, numeric requisition identity, exact public Jobs URL boundary, strict 12-key metadata, `current`/`closed`/null status, Canberra closing-date currentness, numeric-ID ordering, and additive revision `20260914_0004`. | Allow Will and Carmen to implement independently against one Scraper -> DB -> RAG boundary while preventing title/URL-derived identity, inferred deadlines, fixed-term expiry assumptions, or semantic ranking from deciding currentness. | RAG migration/model/repository/API/docs; Will's Jobs normalizer/writer and fixture must remain synchronized. | Qasim (contracts/integration) | Frozen Jobs normalized contract v1 relayed in Day 10 implementation instruction |
| 2026-09-15 | Approve one shared retrieval-unit/embedding/pgvector/hybrid layer; preserve deterministic facts and Courses TF-IDF; add Jobs nullable `role_requirements: list[str]` from direct canonical-page text only; hold Position Description ingestion; and add RAG retrieval support for Accommodation/Support over the approved registry sources with conservative claims. | Add semantic recall without creating vector factual authority, per-domain vector stores, inferred requirements/vacancy/hours, or an API/status change. | RAG migration `20260915_0005`, retrieval services/tests and V6 handoff; Will must synchronize Jobs v2 and new-domain normalized metadata before writes. | Carmen (RAG/backend) | Qasim approval relayed in the V6 implementation instruction |
| 2026-09-15 | Freeze the 2026 Courses family as Course, Program, Major, Minor and Specialisation records in the existing Courses source/domain; use type-sensitive code/year identity, uppercase metadata codes with exact lowercase canonical URL paths, expanded Course/Program fields, shared `SubplanMetadata`, and a Course+Program-only compatibility view. | Persist and retrieve all 1,256 audited entities without disguising Subplans as Programs, splitting storage/vector authority, inventing academic rules/effective dates, or changing the public API. | RAG migration `20260915_0006`, models/repositories/planner/tests and V6 docs; Will must synchronize normalized output/content and coverage evidence. | Carmen (RAG/backend) | Qasim frozen Courses-family addendum relayed in the continuation instruction |


If a decision changes a shared contract, synchronize the affected contract copies and owner handoffs in the same work cycle; each repository owner remains responsible for implementing their side before integration.
